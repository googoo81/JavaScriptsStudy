// 콘솔에게 메세지를 보내는 코드
const a = 9;
const b = 3;
const myName = "googoo81"; //JS는 camelcase방식으로 코드를 작성함

console. log(a * b);
console. log(a + b);
console. log(a / b);
console. log("hello " + myName);
//작은 따옴표('')도 사용가능 주의사항은 시작한걸로 닫아야함
//variable: 값을 저장하거나 유지하는 역할
//constant: 상수(c에선 int)